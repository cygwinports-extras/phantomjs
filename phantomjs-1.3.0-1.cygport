# From the website: "There are two implementations, using C++ and Python."
# Obviously we only need one.
inherit qt4-qmake

DESCRIPTION="Browserless WebKit JavaScript/CoffeeScript interpreter"
HOMEPAGE="http://www.phantomjs.org/"
SRC_URI="http://phantomjs.googlecode.com/files/${P}-source.tar.gz"

src_install() {
	cd ${B}
	dobin bin/phantomjs.exe
}
